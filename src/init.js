/* const e = require("express");
const usuariosPorDefecto = require("../usuarios.json");

const init = async () => {

  const { Usuario } = require("./models");

  const nuevoUsuario = await Usuario.create(usuariosPorDefecto);
};
module.exports = init;
 */
